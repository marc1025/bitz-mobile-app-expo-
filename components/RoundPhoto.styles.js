import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    photo: {
        // resizeMode: 'cover',
        zIndex: 121,
        borderWidth: 2,
        borderColor: 'white',
        marginTop: 3,
        marginLeft: 3,
    },
    photoRing: {
        position: 'absolute',
        zIndex: 21,
    },
});